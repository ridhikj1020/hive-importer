# Server-side only. Never prefix these with NEXT_PUBLIC_ and never commit real values.
# Set the same two variables in Vercel: Project Settings, Environment Variables.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
